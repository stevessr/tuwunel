name: Testing

on:
  workflow_call:
    inputs:
      cargo_profiles:
        type: string
        description: Cargo profiles
      feat_sets:
        type: string
        description: Cargo feature groups
      rust_toolchains:
        type: string
        description: Rust toolchains
      sys_names:
        type: string
        description: System names
      sys_versions:
        type: string
        description: System versions
      rust_targets:
        type: string
        description: Rust targets
      sys_targets:
        type: string
        description: System targets
      machines:
        type: string
        description: Hardware platform vector
      excludes:
        type: string
        default: '[]'
        description: Matrix exclusions
      includes:
        type: string
        default: '[]'
        description: Matrix inclusions
      pipeline:
        type: string
        default: ${{github.event.inputs.pipeline || github.event.workflow_run.head_commit.message}}
      checkout:
        type: string
        default: ${{github.sha}}
      enable_doc:
        type: string
        default: 'true'
      enable_unit:
        type: string
        default: 'true'
      enable_bench:
        type: string
        default: 'true'
      enable_memcheck:
        type: string
        default: 'true'
      enable_smoke:
        type: string
        default: 'true'
      enable_nix:
        type: string
        default: 'true'
      enable_rustsdk:
        type: string
        default: 'true'
      enable_complement:
        type: string
        default: 'true'
      enable_complement_debug:
        type: string
        default: 'true'
      complement_runner:
        type: string
        default: 'het'
      complement_verbose:
        type: string
        default: 'false'
      enable_complement_crypto:
        type: string
        default: 'true'
      complement_crypto_verbose:
        type: string
        default: 'false'
      enable_complement_interop:
        type: string
        default: 'false'
      enable_complement_interop_inverse:
        type: string
        default: 'false'
      complement_interop_peer:
        type: string
        default: 'synapse'
      complement_interop_run:
        type: string
        default: '.*'
      complement_interop_verbose:
        type: string
        default: 'false'
      enable_mas:
        type: string
        default: 'false'
      mas_verbose:
        type: string
        default: 'false'
      enable_playwright:
        type: string
        default: 'false'
      playwright_runner:
        type: string
        default: 'het'
      playwright_workers:
        type: string
        default: '3'
      playwright_retries:
        type: string
        default: '1'
      playwright_shards:
        type: string
        default: '6'

jobs:
  doc:
    if: >
      fromJSON(inputs.enable_doc)
      && !contains(inputs.pipeline, '[ci only it]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["release"]')[0])
      && contains(fromJSON(inputs.feat_sets), fromJSON('["all"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v1-linux-gnu"]')[0])

    name: Doc
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["doc"]'
      cargo_profiles: '["release"]'
      feat_sets: '["all"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v1-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  unit:
    if: >
      fromJSON(inputs.enable_unit)
      && !contains(inputs.pipeline, '[ci only it]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["test"]')[0])
      && contains(fromJSON(inputs.feat_sets), fromJSON('["all"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v1-linux-gnu"]')[0])

    name: Module
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["unit", "integ"]'
      cargo_profiles: '["test"]'
      feat_sets: '["all"]'
      rust_toolchains: ${{inputs.rust_toolchains}}
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v1-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  bench:
    if: >
      fromJSON(inputs.enable_bench)
      && !contains(inputs.pipeline, '[ci only it]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.feat_sets), fromJSON('["all"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Bench
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["unit", "integ"]'
      cargo_profiles: '["bench"]'
      feat_sets: '["all"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  memcheck:
    if: >
      fromJSON(inputs.enable_memcheck)
      && !contains(inputs.pipeline, '[ci only it]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.feat_sets), fromJSON('["all"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Memcheck
    uses: ./.github/workflows/bake.yml
    with:
      #bake_targets: '["unit-valgrind", "integ-valgrind"]'
      bake_targets: '["integ-valgrind"]'
      cargo_profiles: '["bench"]' # use bench not release for debug syms
      feat_sets: '["all"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      includes: ${{inputs.includes}}
      excludes: ${{inputs.excludes}}
      checkout: ${{inputs.checkout}}

  smoke:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_smoke)
      && !contains(inputs.pipeline, '[ci only it]')
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.cargo_profiles
      && inputs.machines

    name: Smoke
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["smoke", "smoke-valgrind"]'
      cargo_profiles: ${{inputs.cargo_profiles}}
      feat_sets: ${{inputs.feat_sets}}
      rust_toolchains: ${{inputs.rust_toolchains}}
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: ${{inputs.sys_targets}}
      machines: ${{inputs.machines}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}
      excludes: >
        [
          {"cargo_profile": "test", "feat_set": "default"},
          {"cargo_profile": "test", "feat_set": "logging"},
          {"cargo_profile": "test", "feat_set": "none", "bake_target": "smoke-valgrind"},
          {"cargo_profile": "test", "rust_toolchain": "stable"},
          {"cargo_profile": "test", "rust_target": "aarch64-unknown-linux-gnu"},
          {"cargo_profile": "test", "sys_target": "x86_64-v2-linux-gnu"},
          {"cargo_profile": "test", "sys_target": "x86_64-v3-linux-gnu"},
          {"cargo_profile": "test", "bake_target": "smoke-valgrind"},
          {"cargo_profile": "bench"},
          {"cargo_profile": "release", "rust_toolchain": "nightly"},
          {"cargo_profile": "release", "rust_toolchain": "stable", "feat_set": "none"},
          {"cargo_profile": "release", "bake_target": "smoke-valgrind"},
          {"cargo_profile": "release-debuginfo", "feat_set": "logging"},
          {"cargo_profile": "release-debuginfo", "rust_toolchain": "nightly"},
          {"cargo_profile": "release-debuginfo", "rust_toolchain": "stable", "feat_set": "none"},
          {"cargo_profile": "release-debuginfo", "bake_target": "smoke"},
          {"cargo_profile": "release-debuginfo", "rust_target": "aarch64-unknown-linux-gnu"},
          {"cargo_profile": "release-native", "rust_toolchain": "stable"},
          {"cargo_profile": "release-native", "feat_set": "none"},
          {"cargo_profile": "release-native", "feat_set": "default"},
          {"cargo_profile": "release-native", "feat_set": "logging"},
          {"cargo_profile": "release-native", "bake_target": "smoke-valgrind"},
          {"cargo_profile": "release-native", "rust_target": "aarch64-unknown-linux-gnu"},
          {"cargo_profile": "release-native", "sys_target": "x86_64-v2-linux-gnu"},
          {"cargo_profile": "release-native", "sys_target": "x86_64-v3-linux-gnu"},
          {"cargo_profile": "release-native", "sys_target": "x86_64-v4-linux-gnu"},
          {"rust_target": "aarch64-unknown-linux-gnu", "feat_set": "none"},
          {"rust_target": "aarch64-unknown-linux-gnu", "feat_set": "default"},
          {"rust_target": "aarch64-unknown-linux-gnu", "sys_target": "x86_64-v1-linux-gnu"},
          {"rust_target": "aarch64-unknown-linux-gnu", "sys_target": "x86_64-v2-linux-gnu"},
          {"rust_target": "aarch64-unknown-linux-gnu", "sys_target": "x86_64-v3-linux-gnu"},
          {"rust_target": "aarch64-unknown-linux-gnu", "sys_target": "x86_64-v4-linux-gnu"},
          {"rust_target": "x86_64-unknown-linux-gnu", "sys_target": "aarch64-v8-linux-gnu"},
          {"sys_target": "aarch64-v8-linux-gnu", "machine": "X64"},
          {"sys_target": "x86_64-v1-linux-gnu", "machine": "ARM64"},
          {"sys_target": "x86_64-v2-linux-gnu", "machine": "ARM64"},
          {"sys_target": "x86_64-v2-linux-gnu", "feat_set": "none"},
          {"sys_target": "x86_64-v2-linux-gnu", "feat_set": "default"},
          {"sys_target": "x86_64-v2-linux-gnu", "feat_set": "logging"},
          {"sys_target": "x86_64-v3-linux-gnu", "machine": "ARM64"},
          {"sys_target": "x86_64-v3-linux-gnu", "feat_set": "none"},
          {"sys_target": "x86_64-v3-linux-gnu", "feat_set": "default"},
          {"sys_target": "x86_64-v3-linux-gnu", "feat_set": "logging"},
          {"sys_target": "x86_64-v3-linux-gnu", "bake_target": "smoke-valgrind"},
          {"sys_target": "x86_64-v4-linux-gnu", "machine": "ARM64"},
          {"sys_target": "x86_64-v4-linux-gnu", "feat_set": "none"},
          {"sys_target": "x86_64-v4-linux-gnu", "feat_set": "default"},
          {"sys_target": "x86_64-v4-linux-gnu", "feat_set": "logging"},
          {"sys_target": "x86_64-v4-linux-gnu", "bake_target": "smoke-valgrind"},
        ]

  nix:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_nix)
      && !contains(inputs.pipeline, '[ci only it]')
      && !contains(inputs.pipeline, '[ci no build]')
      && !contains(github.ref, 'refs/pull')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["release"]')[0])
      && contains(fromJSON(inputs.feat_sets), fromJSON('["all"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["stable"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v1-linux-gnu"]')[0])

    name: Smoke NixOS
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["smoke-nix"]'
      cargo_profiles: '["release"]'
      feat_sets: '["all"]'
      rust_toolchains: '["stable"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v1-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  mas:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_mas)
      && !contains(inputs.pipeline, '[ci no build]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: MAS
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["mas-testee"]'
      cargo_profiles: '["bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      runner: ${{inputs.complement_runner}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  playwright:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_playwright)
      && !contains(inputs.pipeline, '[ci no build]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Playwright
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["playwright-tester", "playwright-testee"]'
      cargo_profiles: '["bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      runner: ${{inputs.playwright_runner}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  complement:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_complement)
      && !contains(inputs.pipeline, '[ci no build]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Complement
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["complement-tester", "complement-testee"]'
      cargo_profiles: '["test", "bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: '["x86_64-unknown-linux-gnu"]'
      sys_targets: '["x86_64-v1-linux-gnu", "x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      runner: ${{inputs.complement_runner}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}
      # Two build cells: debug is test/v1, optimized is bench/v3. The diagonal
      # excludes drop the off-pairs; the ARM64 excludes keep x86 targets on X64.
      excludes: >
        [
          {"cargo_profile": "test", "sys_target": "x86_64-v3-linux-gnu"},
          {"cargo_profile": "bench", "sys_target": "x86_64-v1-linux-gnu"},
          {"sys_target": "x86_64-v1-linux-gnu", "machine": "ARM64"},
          {"sys_target": "x86_64-v3-linux-gnu", "machine": "ARM64"}
        ]

  complement-crypto:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_complement)
      && fromJSON(inputs.enable_complement_crypto)
      && !contains(inputs.pipeline, '[ci no build]')
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Complement-Crypto
    uses: ./.github/workflows/bake.yml
    with:
      bake_targets: '["complement-crypto-tester", "complement-testee"]'
      cargo_profiles: '["bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      runner: ${{inputs.complement_runner}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  rust-sdk-integ:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_rustsdk)
      && !contains(inputs.pipeline, '[ci no build]')
      && contains(fromJSON(inputs.feat_sets), fromJSON('["all"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.rust_targets), fromJSON('["x86_64-unknown-linux-gnu"]')[0])

    name: Matrix SDK Integration
    uses: ./.github/workflows/bake.yml
    with:
      #bake_targets: '["rust-sdk-integ", "rust-sdk-valgrind"]'
      bake_targets: '["rust-sdk-integ"]'
      cargo_profiles: ${{inputs.cargo_profiles}}
      feat_sets: '["all"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: '["x86_64-unknown-linux-gnu"]'
      sys_targets: ${{inputs.sys_targets}}
      machines: '["X64"]'
      runner: ${{inputs.complement_runner}}
      checkout: ${{inputs.checkout}}
      includes: ${{inputs.includes}}
      artifact: >
        {
          "rust-sdk-integ": {
            "src": "/var/log/tuwunel.log",
            "dst": "rust-sdk-integ.tuwunel.log",
          },
          "rust-sdk-valgrind": {
            "src": "/var/log/tuwunel.log",
            "dst": "rust-sdk-valgrind.tuwunel.log",
          }
        }
      excludes: >
        [
          {"bake_target": "rust-sdk-valgrind", "cargo_profile": "test"},
          {"feat_set": "none"},
          {"feat_set": "logging"},
          {"cargo_profile": "release"},
          {"cargo_profile": "release-debuginfo"},
          {"cargo_profile": "release-native"},
          {"cargo_profile": "test", "sys_target": "x86_64-v2-linux-gnu"},
          {"cargo_profile": "test", "sys_target": "x86_64-v3-linux-gnu"},
          {"cargo_profile": "bench", "sys_target": "x86_64-v1-linux-gnu"},
          {"cargo_profile": "bench", "sys_target": "x86_64-v2-linux-gnu"},
          {"rust_target": "aarch64-unknown-linux-gnu"},
          {"sys_target": "aarch64-v8-linux-gnu"},
        ]

  acceptance-init:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_playwright)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])
      && inputs.playwright_shards > 1

    name: Playwright Sharding
    runs-on: ["${{fromJSON(inputs.machines)[0]}}", "${{inputs.playwright_runner}}"]

    outputs:
      ids: ${{steps.gen.outputs.ids}}

    steps:
    - id: gen
      name: Generate Shard Matrix
      env:
        shards: ${{inputs.playwright_shards}}
      run: |
        case "$shards" in ''|*[!0-9]*) echo "invalid shards: '$shards'" >&2; exit 1;; esac
        test "$shards" -ge 1 || { echo "shards must be >= 1" >&2; exit 1; }
        echo "ids=[$(seq -s, 1 "$shards")]" >> "$GITHUB_OUTPUT"

  acceptance:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_playwright)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Acceptance (element-web)
    needs: [playwright, acceptance-init]
    uses: ./.github/workflows/acceptance.yml
    with:
      counterparty: element-web
      runner: ${{inputs.playwright_runner}}
      workers: ${{inputs.playwright_workers}}
      retries: ${{inputs.playwright_retries}}
      shards: ${{inputs.playwright_shards}}
      shard_ids: ${{needs.acceptance-init.outputs.ids || '[1]'}}
      cargo_profiles: '["bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  authentication:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_mas)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Authentication (mas)
    needs: [mas]

    uses: ./.github/workflows/compliance.yml
    with:
      regime: authentication
      counterparty: mas
      runner: ${{inputs.complement_runner}}
      script: docker/mas.sh
      summariser: .github/workflows/summarise_mas.sh
      results_path: tests/mas/results.jsonl
      logs_path: tests/mas/tuwunel.log
      runtime_metrics_path: tests/mas/mas.log
      verbose: ${{inputs.mas_verbose}}
      results_artifact_prefix: mas_results
      output_artifact_prefix: mas_output
      metrics_artifact_prefix: mas_worker_log
      runtime_history: false
      cargo_profiles: '["bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  compliance:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_complement)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Compliance (tuwunel)
    needs: [complement]

    uses: ./.github/workflows/compliance.yml
    with:
      regime: compliance
      counterparty: tuwunel
      runner: ${{inputs.complement_runner}}
      script: docker/complement.sh
      summariser: .github/workflows/summarise_complement_complement.sh
      results_path: tests/complement/results.jsonl
      logs_path: tests/complement/logs.jsonl
      runtime_metrics_path: tests/complement/runtime_metrics.tar.zst
      verbose: ${{inputs.complement_verbose}}
      results_artifact_prefix: complement_results
      output_artifact_prefix: complement_output
      metrics_artifact_prefix: complement_runtime_metrics
      runtime_history: true
      complement_perf: true
      cargo_profiles: '["bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  compliance-debug:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_complement)
      && fromJSON(inputs.enable_complement_debug)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["test"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v1-linux-gnu"]')[0])

    name: Compliance (tuwunel)
    needs: [complement]

    uses: ./.github/workflows/compliance.yml
    with:
      regime: compliance
      counterparty: tuwunel
      runner: ${{inputs.complement_runner}}
      script: docker/complement.sh
      summariser: .github/workflows/summarise_complement_complement.sh
      results_path: tests/complement/results.jsonl
      logs_path: tests/complement/logs.jsonl
      runtime_metrics_path: tests/complement/runtime_metrics.tar.zst
      verbose: ${{inputs.complement_verbose}}
      results_artifact_prefix: complement_debug_results
      output_artifact_prefix: complement_debug_output
      metrics_artifact_prefix: complement_debug_runtime_metrics
      cargo_profiles: '["test"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v1-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  compliance-crypto:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_complement)
      && fromJSON(inputs.enable_complement_crypto)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["bench"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v3-linux-gnu"]')[0])

    name: Cryptography (rust-sdk)
    needs: [complement-crypto]

    uses: ./.github/workflows/compliance.yml
    with:
      regime: crypto
      counterparty: rust-sdk
      runner: ${{inputs.complement_runner}}
      script: docker/complement-crypto.sh
      summariser: .github/workflows/summarise_complement_crypto.sh
      results_path: tests/complement-crypto/results.jsonl
      logs_path: tests/complement-crypto/logs.jsonl
      runtime_metrics_path: tests/complement-crypto/runtime_metrics.tar.zst
      verbose: ${{inputs.complement_crypto_verbose}}
      results_artifact_prefix: complement_crypto_results
      output_artifact_prefix: complement_crypto_output
      metrics_artifact_prefix: complement_crypto_runtime_metrics
      cargo_profiles: '["bench"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v3-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  compliance-interop:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_complement)
      && fromJSON(inputs.enable_complement_debug)
      && fromJSON(inputs.enable_complement_interop)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["test"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v1-linux-gnu"]')[0])

    name: Interoperability (${{inputs.complement_interop_peer}})
    needs: [complement]

    uses: ./.github/workflows/compliance.yml
    with:
      regime: interop
      counterparty: ${{inputs.complement_interop_peer}}
      runner: ${{inputs.complement_runner}}
      script: interop=${{inputs.complement_interop_peer}} docker/complement.sh "${{inputs.complement_interop_run}}"
      summariser: .github/workflows/summarise_complement_interop.sh
      results_path: tests/complement/interop/results.jsonl
      logs_path: tests/complement/interop/logs.jsonl
      runtime_metrics_path: tests/complement/interop/runtime_metrics.tar.zst
      verbose: ${{inputs.complement_interop_verbose}}
      results_artifact_prefix: complement_interop_results
      output_artifact_prefix: complement_interop_output
      metrics_artifact_prefix: complement_interop_runtime_metrics
      cargo_profiles: '["test"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v1-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}

  compliance-interop-inverse:
    if: >
      !failure() && !cancelled()
      && fromJSON(inputs.enable_complement)
      && fromJSON(inputs.enable_complement_debug)
      && fromJSON(inputs.enable_complement_interop)
      && fromJSON(inputs.enable_complement_interop_inverse)
      && !contains(inputs.pipeline, '[ci no build]')
      && inputs.machines
      && contains(fromJSON(inputs.cargo_profiles), fromJSON('["test"]')[0])
      && contains(fromJSON(inputs.rust_toolchains), fromJSON('["nightly"]')[0])
      && contains(fromJSON(inputs.sys_targets), fromJSON('["x86_64-v1-linux-gnu"]')[0])

    name: Interoperability Inverted (${{inputs.complement_interop_peer}})
    needs: [complement]

    uses: ./.github/workflows/compliance.yml
    with:
      regime: interop
      counterparty: ${{inputs.complement_interop_peer}}
      runner: ${{inputs.complement_runner}}
      script: interop_hs=hs1 interop=${{inputs.complement_interop_peer}} docker/complement.sh "${{inputs.complement_interop_run}}"
      summariser: .github/workflows/summarise_complement_interop.sh
      results_path: tests/complement/interop/results.jsonl
      logs_path: tests/complement/interop/logs.jsonl
      runtime_metrics_path: tests/complement/interop/runtime_metrics.tar.zst
      verbose: ${{inputs.complement_interop_verbose}}
      results_artifact_prefix: complement_interop_inverse_results
      output_artifact_prefix: complement_interop_inverse_output
      metrics_artifact_prefix: complement_interop_inverse_runtime_metrics
      cargo_profiles: '["test"]'
      feat_sets: '["logging"]'
      rust_toolchains: '["nightly"]'
      sys_names: ${{inputs.sys_names}}
      sys_versions: ${{inputs.sys_versions}}
      rust_targets: ${{inputs.rust_targets}}
      sys_targets: '["x86_64-v1-linux-gnu"]'
      machines: ${{inputs.machines}}
      excludes: ${{inputs.excludes}}
      includes: ${{inputs.includes}}
      checkout: ${{inputs.checkout}}
